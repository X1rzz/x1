
# Supabase settings — copy to .env and set your values
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SCHEMA=public
SUPABASE_TABLE=transactions
# Optional: static user id for demo (UUID). If you enable RLS with user_id policy and no Auth/JWT, 
# you can set a fixed UUID here and policies that allow this UUID (for testing only).
DEMO_USER_ID=00000000-0000-0000-0000-000000000000
